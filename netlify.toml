[build]
  command = "vite build"
  publish = "dist"

# SPA fallback: all routes serve index.html so React Router works on deep links / refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
