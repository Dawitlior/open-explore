/**
 * Phase 4 — Dashboard Engine barrel
 * Pure data hooks. Zero JSX. Consumed by UI renderer components.
 */
export { useDashboardData, type DashboardData } from './use-dashboard-data';
export { useExpectancyMode, type ExpectancyMode, type ExpectancyState } from './use-expectancy-mode';
export { useChartGuard } from './use-chart-guard';
